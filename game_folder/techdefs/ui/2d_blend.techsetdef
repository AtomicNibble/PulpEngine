#include <2d_base>

Technique( "unlit" )
{
	state = "blend + nocull"

	vs = VertexShader()
	{
		source = "Prim.hlsl"
		entry = "PrimVS"
	}
	ps = PixelShader()
	{
		source = "Prim.hlsl"
		entry = "PrimPS"
	}
}
